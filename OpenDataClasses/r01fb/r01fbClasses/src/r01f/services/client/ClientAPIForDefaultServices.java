package r01f.services.client;






/**
 * Marker interface that states that the API is calling the REST, Bean, EJB, etc services through a proxy
 * depending on the configuration
 * @see ClientAPI
 */
public interface ClientAPIForDefaultServices 
     	 extends ClientAPI {
	/* just a marker interface */
}
